<h1>Edit <%="#{@registered_application.name}"%></h1>
 
 <div class="row">
   <div class="col-md-8">
   <%= render partial: 'form', locals: { user: @registered_application.user, registered_application: @registered_application } %>
   </div>
 </div>